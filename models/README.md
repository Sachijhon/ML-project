# Model Artifacts

The trained Random Forest model used by `app.py` is expected at:

```text
models/rf_heart_model.pkl
```

The serialized model is a binary artifact and should be added to this directory when publishing the complete runnable project. Do not replace it with a text or base64 copy unless `app.py` is updated accordingly.
